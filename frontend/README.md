# Frontend

- `index.html` — Telegram Mini App / customer UI
- `admin/index.html` — Admin UI
- `instructor/index.html` — Instructor UI
- `assets/` — shared static assets

All authentication and authorization must be enforced by the API. Do not put secrets in frontend files.
